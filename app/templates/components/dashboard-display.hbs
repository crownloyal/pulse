<div class="offset-by-three nine columns dashboard-wrapper row">
	<div class="eleven columns">
		<h3>{{model.name}}</h3>
		<p>{{model.text}}</p>
	</div>
	<div class="one column">
	<button {{action "toggleEditDashboard"}}>Edit me!</button>
	{{#link-to 'dashboard.board.edit' model.id action="toggleEditDashboard"}}Edit{{/link-to}}
	</div>
</div>
